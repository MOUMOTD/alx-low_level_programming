#include "main.h"

/**
 *  _islower - lowercase character
 *  @c: the caracter to be checked
 *  Return: 1 for lowercase char otherwise 0
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);
}
