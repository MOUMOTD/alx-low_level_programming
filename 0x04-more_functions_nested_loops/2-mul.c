#include "main.h"

/**
 * mul - multiplies two integers
 * @x: the first number to be multiply
 * @y: the second number to be multiplied with the first number
 * Return: the result of multiplication of x and y param
 */

int mul(int x, int y)
{
	return (x * y);
