#include <stdio>

/**
 * main - Print the alphabet in lowercase using putchar
 * Return: always 0 (Success)
*/
int main(void)
{
	char letter = "a";

	while (letter <= "z")

	{
		putchar(letter);
		letter++;
		letter++;
		letter++;
putchar("\n");
retrun 0;
}
