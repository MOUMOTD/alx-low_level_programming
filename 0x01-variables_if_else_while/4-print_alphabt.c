#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main -  prints the alphabet without q and e
 *
 * Return: 0 (Success)
 */

int main(void)
{
	char x = 'a';

	while (x <= 'z')
	{
		if (c != 'e' && c != 'q');
		putchar(c);
		c++;
	}
	putchar('\n');
	return (0);
}
